@using Gr8_VehicleManagement.Common.Helpers
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Gr8 Vehicle Dealer</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
</head>
<body>
    <header>
        <nav class="navbar navbar-expand-lg navbar-light bg-white fixed-top">
            <div class="container">
                <a class="navbar-brand fw-bold" asp-area="" asp-page="/Index">
                    <i class="bi bi-car-front me-2"></i>Gr8 Vehicle Dealer
                </a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target=".navbar-collapse">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="navbar-collapse collapse">
                    <ul class="navbar-nav me-auto">
                        <li class="nav-item">
                            <a class="nav-link" asp-area="" asp-page="/Index">
                                <i class="bi bi-house me-1"></i>Trang Chủ
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" asp-area="" asp-page="/Vehicles/Index">
                                <i class="bi bi-car-front me-1"></i>Xe
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" asp-area="" asp-page="/Customers/Index">
                                <i class="bi bi-people me-1"></i>Khách Hàng
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" asp-area="" asp-page="/Orders/Index">
                                <i class="bi bi-cart me-1"></i>Đơn Hàng
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" asp-area="" asp-page="/Payments/Index">
                                <i class="bi bi-credit-card me-1"></i>Thanh Toán
                            </a>
                        </li>
                    </ul>
                    <ul class="navbar-nav">
                        @if (Context.Session.IsAuthenticated())
                        {
                          @if (!Context.Session.IsCustomer())
                          {
                            <li class="nav-item">
                                <a class="nav-link" asp-area="" asp-page="/Dashboard/Index">
                                    <i class="bi bi-speedometer2 me-1"></i>Dashboard
                                </a>
                            </li>
                          }
                            <li class="nav-item">
                                <a class="nav-link" asp-area="" asp-page="/Account/Profile">
                                    <i class="bi bi-person-circle me-1"></i>Profile
                                </a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" asp-area="" asp-page="/Account/Logout">
                                    <i class="bi bi-box-arrow-right me-1"></i>Đăng Xuất
                                </a>
                            </li>
                        }
                        else
                        {
                            <li class="nav-item">
                                <a class="nav-link" asp-area="" asp-page="/Account/Login">
                                    <i class="bi bi-box-arrow-in-right me-1"></i>Đăng Nhập
                                </a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" asp-area="" asp-page="/Account/Register">
                                    <i class="bi bi-person-plus me-1"></i>Đăng Ký
                                </a>
                            </li>
                        }
                    </ul>
                </div>
            </div>
        </nav>
    </header>

    <main class="main-content">
        @RenderBody()
    </main>

    @* <footer class="footer">
        <div class="container">
            <div class="row">
                <div class="col-md-6">
                    <h5 class="text-warning mb-3">
                        <i class="bi bi-car-front me-2"></i>Gr8 Vehicle Dealer
                    </h5>
                    <p class="mb-0">Hệ thống quản lý đại lý xe điện hiện đại và chuyên nghiệp</p>
                </div>
                <div class="col-md-6 text-md-end">
                    <div class="mb-2">
                        <a asp-page="/Contact" class="text-warning me-3">
                            <i class="bi bi-envelope me-1"></i>Liên hệ
                        </a>
                        <a asp-page="/Privacy" class="text-warning me-3">
                            <i class="bi bi-shield-check me-1"></i>Bảo mật
                        </a>
                        <a asp-page="/About" class="text-warning">
                            <i class="bi bi-info-circle me-1"></i>Giới thiệu
                        </a>
                    </div>
                    <small class="text-white-50">© 2025 - Gr8 Vehicle Dealer System</small>
                </div>
            </div>
        </div>
    </footer> *@

    <!-- Refreshed footer: cleaner structure, responsive columns, call-to-action and socials -->
    <footer class="footer">
        <div class="container">
            <div class="row gy-4 align-items-center">
                <div class="col-md-4">
                    <a class="navbar-brand fw-bold d-inline-flex align-items-center text-warning" asp-area="" asp-page="/Index">
                        <i class="bi bi-car-front me-2"></i>Gr8 Vehicle Dealer
                    </a>
                    <p class="text-white-50 mt-2 mb-0">Hệ thống quản lý đại lý xe điện hiện đại và chuyên nghiệp</p>
                </div>

                <div class="col-md-4">
                    <div class="row">
                        <div class="col-6">
                            <h6 class="text-warning mb-2">Sản phẩm</h6>
                            <ul class="list-unstyled footer-links mb-0">
                                <li><a asp-page="/Vehicles/Index">Xe</a></li>
                                <li><a asp-page="/Orders/Index">Đơn Hàng</a></li>
                                <li><a asp-page="/Payments/Index">Thanh Toán</a></li>
                            </ul>
                        </div>
                        <div class="col-6">
                            <h6 class="text-warning mb-2">Công ty</h6>
                            <ul class="list-unstyled footer-links mb-0">
                                <li><a asp-page="/About">Giới thiệu</a></li>
                                <li><a asp-page="/Contact">Liên hệ</a></li>
                                <li><a asp-page="/Privacy">Bảo mật</a></li>
                            </ul>
                        </div>
                    </div>
                </div>

                <div class="col-md-4 text-md-end">
                    <div class="mb-2">
                        <a class="btn btn-outline-light btn-sm me-2" asp-page="/Contact"><i class="bi bi-envelope me-1"></i>Liên hệ</a>
                        <a class="btn btn-outline-light btn-sm" asp-page="/Privacy"><i class="bi bi-shield-check me-1"></i>Bảo mật</a>
                    </div>

                    <div class="d-flex justify-content-md-end gap-3 mt-2">
                        <a class="text-white-50 fs-5" href="#" aria-label="Facebook"><i class="bi bi-facebook"></i></a>
                        <a class="text-white-50 fs-5" href="#" aria-label="Twitter"><i class="bi bi-twitter"></i></a>
                        <a class="text-white-50 fs-5" href="#" aria-label="LinkedIn"><i class="bi bi-linkedin"></i></a>
                    </div>
                </div>
            </div>

            <hr class="border-light my-4" />

            <div class="d-flex flex-column flex-md-row justify-content-between align-items-center small">
                <div class="text-white-50">© 2025 - Gr8 Vehicle Dealer System</div>
                <div class="mt-2 mt-md-0">
                    <a class="text-white-50 me-3" asp-page="/Contact"><i class="bi bi-envelope"></i> Liên hệ</a>
                    <a class="text-white-50 me-3" asp-page="/Privacy"><i class="bi bi-shield-check"></i> Bảo mật</a>
                </div>
            </div>
        </div>
    </footer>

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    
    <!-- Toast Notifications -->
    <script>
        // Toast notification system
        function showToast(message, type = 'info') {
            const toastContainer = document.getElementById('toast-container') || createToastContainer();
            const toastId = 'toast-' + Date.now();
            
            const toastHtml = `
                <div id="${toastId}" class="toast align-items-center text-white bg-${type} border-0" role="alert" aria-live="assertive" aria-atomic="true">
                    <div class="d-flex">
                        <div class="toast-body">
                            <i class="bi bi-${getToastIcon(type)} me-2"></i>
                            ${message}
                        </div>
                        <button type="button" class="btn-close btn-close-white me-2 m-auto" data-bs-dismiss="toast" aria-label="Close"></button>
                    </div>
                </div>
            `;
            
            toastContainer.insertAdjacentHTML('beforeend', toastHtml);
            const toastElement = document.getElementById(toastId);
            const toast = new bootstrap.Toast(toastElement, { delay: 5000 });
            toast.show();
            
            // Auto remove after 5 seconds
            setTimeout(() => {
                if (toastElement && toastElement.parentNode) {
                    toastElement.parentNode.removeChild(toastElement);
                }
            }, 5000);
        }
        
        function createToastContainer() {
            const container = document.createElement('div');
            container.id = 'toast-container';
            container.className = 'toast-container position-fixed top-0 end-0 p-3';
            container.style.zIndex = '9999';
            document.body.appendChild(container);
            return container;
        }
        
        function getToastIcon(type) {
            const icons = {
                'success': 'check-circle',
                'error': 'exclamation-triangle',
                'warning': 'exclamation-triangle',
                'info': 'info-circle'
            };
            return icons[type] || 'info-circle';
        }
        
        // Loading indicator
        function showLoading(element) {
            if (element) {
                element.disabled = true;
                const originalText = element.innerHTML;
                element.innerHTML = '<span class="spinner-border spinner-border-sm me-2"></span>Đang xử lý...';
                element.setAttribute('data-original-text', originalText);
            }
        }
        
        function hideLoading(element) {
            if (element && element.hasAttribute('data-original-text')) {
                element.disabled = false;
                element.innerHTML = element.getAttribute('data-original-text');
                element.removeAttribute('data-original-text');
            }
        }
        
        // Global error handler
        window.addEventListener('error', function(e) {
            console.error('Global error:', e.error);
            showToast('Đã xảy ra lỗi không mong muốn. Vui lòng thử lại.', 'error');
        });
        
        // Form validation enhancement
        document.addEventListener('DOMContentLoaded', function() {
            // Add loading to all form submits
            const forms = document.querySelectorAll('form');
            forms.forEach(form => {
                form.addEventListener('submit', function(e) {
                    const submitBtn = form.querySelector('button[type="submit"]');
                    if (submitBtn) {
                        showLoading(submitBtn);
                    }
                });
            });
            
            // Show success/error messages from TempData
            @if (TempData["SuccessMessage"] != null)
            {
                <text>showToast('@Html.Raw(TempData["SuccessMessage"])', 'success');</text>
            }
            @if (TempData["ErrorMessage"] != null)
            {
                <text>showToast('@Html.Raw(TempData["ErrorMessage"])', 'error');</text>
            }
            @if (TempData["WarningMessage"] != null)
            {
                <text>showToast('@Html.Raw(TempData["WarningMessage"])', 'warning');</text>
            }
            @if (TempData["InfoMessage"] != null)
            {
                <text>showToast('@Html.Raw(TempData["InfoMessage"])', 'info');</text>
            }
        });
    </script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>